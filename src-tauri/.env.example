# Google OAuth Configuration
# These are used for local development. For CI builds, set them as GitHub repository secrets.
#
# Setup Instructions:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 credentials (Web application type)
# 3. Add http://localhost:3847/auth/callback as an authorized redirect URI
# 4. Copy the credentials below for local development
#
# For GitHub Actions builds:
# - Set GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET as repository secrets
# - The workflow will embed them at compile time

GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here
